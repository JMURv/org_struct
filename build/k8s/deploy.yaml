apiVersion: apps/v1
kind: Deployment

metadata:
  name: org-struct
  labels:
    app: org-struct

spec:
  replicas: 1
  selector:
    matchLabels:
      app: org-struct
  template:
    metadata:
      labels:
        app: org-struct
    spec:
      containers:
        - name: app
          image: orgstruct:latest

          envFrom:
            - configMapRef:
                name: org-struct-cfg
            - secretRef:
                name: org-struct-secret

          ports:
            - name: http
              containerPort: 8080
            - name: metrics
              containerPort: 8085
            - name: grpc
              containerPort: 50050

          livenessProbe:
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            httpGet:
              path: /health
              port: 8080

          readinessProbe:
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            httpGet:
              path: /health
              port: 8080

---
apiVersion: v1
kind: Service

metadata:
  name: org-struct
  labels:
    app: org-struct

spec:
  selector:
    app: org-struct
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080

    - name: grpc
      port: 50050
      targetPort: 50050
      nodePort: 30050

    - name: metrics
      port: 8085
      targetPort: 8085
      nodePort: 30085
  type: NodePort
